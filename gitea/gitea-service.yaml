apiVersion: v1
kind: Service
metadata:
  name: gitea-service
  namespace: devops
spec:
  selector:
    app: gitea
  ports:
    - name: gitea-ui
      protocol: TCP
      port: 6666
      targetPort: 3000
